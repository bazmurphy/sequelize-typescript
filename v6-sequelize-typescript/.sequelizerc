const path = require("path");

module.exports = {
  config: path.resolve("dist", "config", "config.js"),
  "models-path": path.resolve("dist", "models"),
  "seeders-path": path.resolve("dist", "seeders"), // can use src
  "migrations-path": path.resolve("dist", "migrations"), // can use src
};
